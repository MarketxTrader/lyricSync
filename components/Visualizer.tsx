import React from 'react';

// This component is deprecated in the new UI design but kept to prevent import errors during transition if cached.
const Visualizer: React.FC = () => {
  return null;
};

export default Visualizer;